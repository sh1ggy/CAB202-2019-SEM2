#include <stdio.h>

// everything in c happens in functions, main() being the starting point
int main() 
{
    printf("\fhello\n");
    return 0; // function main() promises to return an int
}
